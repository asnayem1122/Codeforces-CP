// Problem Code: 231A
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int t; cin >> t;

    int rows=t ,cols=3;

    vector<vector<int>>grid(rows,vector<int>(cols,0));
    for(int i=0;i<t;i++){
        for(int j=0;j<cols;j++){
            cin>> grid[i][j];
            
        }
    }

    for(int i: x){
        if(x[i][0]+)
    }
    return 0;
}